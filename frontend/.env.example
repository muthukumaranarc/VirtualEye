# ──────────────────────────────────────────────────────────────
# VirtualEye Frontend — Environment Variables
# Copy this file to .env and fill in the values.
# DO NOT commit the .env file to version control.
# ──────────────────────────────────────────────────────────────

# Base URL of the VirtualEye Flask backend
# Vite exposes all variables prefixed with VITE_ to the browser
VITE_VIRTUALEYE_BACKEND_URL=http://localhost:5000
